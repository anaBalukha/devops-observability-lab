global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: "observability-app"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:5000"]